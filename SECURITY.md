# Security

This MVP stores user data on the device and makes no network requests. Export files contain the user's complete tracking history and should be handled as sensitive personal data.

Report vulnerabilities privately to the project maintainers. Do not include real personal exports in public issues. The MVP does not store API keys; a future model-provider feature must use a platform secret-store adapter and must never include secrets in exports.
